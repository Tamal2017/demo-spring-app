# use an in-memory database for testing instead of PostgreSQL
spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:

# JWT
jwt:
  secret: test
  expiration:
    ms: 1000
